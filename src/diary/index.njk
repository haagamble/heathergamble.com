<h1>Language Learning Diary</h1>
<p>Weekly notes on what I’m trying, what works, and what I’m learning.</p>

{% for post in collections.diary | reverse %}
  <article>
    <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
    <p><small>{{ post.date.toDateString() }}</small></p>

    {% if post.data.page.excerpt %}
      <div>{{ post.data.page.excerpt | safe }}</div>
    {% elseif post.data.summary %}
      <p>{{ post.data.summary }}</p>
    {% endif %}

    <p><a href="{{ post.url }}">Read more →</a></p>
    <hr>
  </article>
{% endfor %}

<p><a href="/">← Home</a></p>
